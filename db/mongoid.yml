development:
    host: localhost
    database: monitrb
    logger: false
    # autocreate_indexes: true

production:
    host: <%= ENV['MONGOID_HOST'] %>
    # port: <%= ENV['MONGOID_PORT'] %>
    # username: <%= ENV['MONGOID_USERNAME'] %>
    # password: <%= ENV['MONGOID_PASSWORD'] %>
    database: <%= ENV['MONGOID_DATABASE'] %>
    logger: false
    # autocreate_indexes: true

test:
    host: localhost
    database: monitrb_test
